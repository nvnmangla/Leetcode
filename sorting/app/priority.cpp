#include "utils.hpp"
/**
 ** Priority Queues
 *      - Data structures, that maintain the element of set S
 *      - elment of S has a key
 **    - Operations
 *          -  Insert(S,x) - x->S
 *          - Maximum(S) - return elem, with largest key
 *          - Extract-Max(S) - remove and return elem, with largest key
 *          - Increase-Key(S,i,k)- to increase elemnt key to k
 */

// Lets see these functions

int maximum(vector<int>& A);
void increase_key(vector<int>& A, int i, int k);
int extract_max(vector<int>& A);
void insert_key(vector<int>& A, int key);

int main() { return 0; }
